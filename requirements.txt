# Core ML / Vision
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.45.0
peft>=0.12.0              # LoRA fine-tuning
bitsandbytes>=0.43.0      # 4-bit quantization (QLoRA)
accelerate>=0.30.0        # Multi-GPU / DeepSpeed support
flash-attn>=2.5.0         # Flash Attention 2 (install separately, see below)

# Video processing
opencv-python>=4.9.0
decord>=0.6.0             # Fast video reader (used by Qwen2.5-VL)
imageio>=2.34.0
moviepy>=1.0.3

# Dataset & Hub
datasets>=2.20.0
huggingface-hub>=0.23.0

# Knowledge Graph
networkx>=3.3             # Graph construction and manipulation
pyvis>=0.3.2              # Interactive graph visualization
rdflib>=7.0.0             # RDF/ontology support

# Graph Neural Network
torch-geometric>=2.5.0   # PyTorch Geometric for GNNs

# Object Detection (for TSKG construction)
ultralytics>=8.2.0        # YOLOv11

# Experiment Tracking
wandb>=0.17.0             # Weights & Biases
# mlflow>=2.13.0          # Alternative (uncomment if preferred)

# Evaluation
evaluate>=0.4.0
scikit-learn>=1.4.0
scipy>=1.13.0

# Data handling
numpy>=1.26.0
pandas>=2.2.0
Pillow>=10.3.0
tqdm>=4.66.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0            # Interactive plots
jupyter>=1.0.0
ipywidgets>=8.1.0

# Config management
hydra-core>=1.3.0
omegaconf>=2.3.0

# Utilities
python-dotenv>=1.0.0      # .env loading for API keys
loguru>=0.7.0             # Better logging

# ⚠️ Flash Attention must be installed separately:
# pip install flash-attn --no-build-isolation
# (Requires CUDA toolkit installed on the machine)
